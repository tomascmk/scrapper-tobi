For running this script please:

1. Install:

    Node
    Npm

2. Open a command line in the proyect's root

3. run the script "npm i" to instal all the dependencies

4. run the script "node scrapper.js" to start the script